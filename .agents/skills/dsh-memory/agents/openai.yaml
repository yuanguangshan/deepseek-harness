interface:
  display_name: "DSH Memory"
  short_description: "Cross-session long-term memory (five markdown tracks) for the DeepSeek Harness"
  default_prompt: "Use $dsh-memory to record or recall durable facts across sessions, projects, and workspaces."
