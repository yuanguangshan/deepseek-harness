interface:
  display_name: "DSH Usage"
  short_description: "API usage/quota status (opencode go windows + DeepSeek balance) for the DeepSeek Harness"
  default_prompt: "Use $dsh-usage to check API quota or balance before or after expensive runs."
