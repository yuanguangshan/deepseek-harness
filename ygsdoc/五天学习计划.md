# DeepSeek Harness 五天学习计划

> 循序渐进的五天线：先跑通 → 懂架构 → 深入子系统 → 亲手写插件。
> 主打"跟 agent 结对 + 读 `docs/` + 动手尝试"。

## 学习原则

- **先跑通，再追原理**：每天先动手运行/看效果，再读对应源码
- **跟 agent 学习**：项目官方文档就建议"用 agent 帮你探索代码库"
- **随时用 `git log` 看演进**：每个提交都是一个决策案例

---

## Day 1 · 跑起来 + 摸清整体 🚀

**目标**：让 dsh 可控地跑起来，建立"这到底是个啥"的全景。

- [ ] `pnpm install` + `pnpm run build`（踩一遍环境）
- [ ] 分三端跑：`pnpm dsh web`（GUI）、`pnpm dsh --profile headless "..."`（无头）、`dsh-tui`（终端 UI），对比差异
- [ ] 读 `docs/README.md` + `packages/README.md`，建 package 地图
- [ ] **核心**：通读 `docs/architecture.md` + `docs/cordis-primer.md`（5 个 Cordis 概念）
- [ ] `pnpm dsh --profile web --dump-config` 看运行时插件树

**产出**：能说清 dsh = "一切皆插件 + Cordis 内核"，知道 `packages/` 分几大组。

---

## Day 2 · 会话日志（单一事实来源）📜

**目标**：理解 dsh 最核心设计——**事件溯源日志**。

- [ ] GUI 跑一次对话，找到 `~/.dsh/sessions/*.jsonl`
- [ ] 对照 `docs/subsystems/session.md` 通读 `SessionEventMap`
- [ ] 理解"**模型可见 ⟺ 已记录**"
- [ ] 看 `packages/core/session/src/types.ts` 源码
- [ ] 动手：写脚本读 session.jsonl，画出 tool 调用链

**产出**：能看懂会话日志，理解"LLM 历史是从日志 derive"。

---

## Day 3 · 能力接缝（Capability Seams）🔌

**目标**：理解扩展哲学——"换一个 Provider 就改整个产品"。

- [ ] 通读 `docs/capability-seams.md`
- [ ] 吃透一个 seam 的三件套（Service Definition / Provider / Consumer）
  - 推荐 **shell**（dsh-shell + dsh-bash-local + dsh-tool-bash）
  - 或 **fs**（dsh-fs + fs-*/sandbox/e2b + tool-fs）
- [ ] 对照自己的 `dsh-wechat` / `dsh-memory`
- [ ] 看工具执行管线 `docs/tool-execution-pipeline.md`

**产出**：能讲清"seam 是什么、它为什么值钱、怎么切换 Provider"。

---

## Day 4 · 核心子系统 Deep-dive 🔥

**目标**：横向追 2-3 个亮点子系统（都是最惊艳的）。

- [ ] **工具执行管线**：`packages/core/tools/src/index.ts`
- [ ] **LLM seam**：`packages/llm/llm/src/types.ts`（StreamChunk、adapter 契约）
- [ ] **Compaction**：`docs/subsystems/compaction.md` 的 `SurfaceOp {op:'replace'}`
- [ ] **持久化/崩溃恢复**：`docs/subsystems/persistence.md`

**每个**：读 subsystem 文档 → 读源码 → 跑最小例。

**产出**：对 2-3 个子系统能"画出数据流、说出不变式"。

---

## Day 5 · 写自己的插件（从学到用）📦

**目标**：亲手写一个规范化插件（你已写 dsh-memory/dsh-wechat//text2card）。

- [ ] 按 `docs/cookbook/adding-a-package.md` 完整走流程
- [ ] 做一个模型工具（`ctx.tools.register`）或能力 seam
- [ ] 遵循门槛：per-file 100% 覆盖、`./invariant`、Agent Note
- [ ] 读一条真实 Agent Note 看"决策记录"怎么写
- [ ] 跑 `pnpm run test:coverage` / `pnpm run test:snapshot`

**产出**：一个可提交、可测试、按规范写的插件。

---

## 贯穿全程的辅助

- `docs/` 是金矿，`docs/cookbook/` 是实操手册
- 生成图谱：`docs/module-graph.md`、`docs/capability-seams.md`
- `.agents/notes/implemented/` 是"决策的为什么"
- 随时跟 agent 结对："读这段代码讲给我听"
