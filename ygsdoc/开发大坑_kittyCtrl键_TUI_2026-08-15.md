# 一个 Ctrl+U 不生效的 Bug，让我绕了一大圈（记一次 TUI 开发的"大坑"）

> 广山哥 2026-08-15 记录

从给 `dsh-repl`（DeepSeek Harness 的 TUI）加"上下箭头翻历史""常用行编辑快捷键"功能开始，我撞上一个极其隐蔽的坑，前后绕了好几个小时，几乎把代码、终端、库都怀疑了个遍。

## 起因

想给 TUI 增加行编辑便利性：`Ctrl+U`（删到行首）、`Ctrl+K`（删到行尾）、`Ctrl+A`（到行首）、`Ctrl+E`（到行尾）、`Ctrl+W`（删前词）这些 shell 里很常用的键。

结果发现：**这些 Ctrl 组合键一个都不生效**，而方向键（左右）都是正常的。

## 排查的一连串猜测（全是弯路）

1. **先怀疑 TUI 命令补全**：以为是 `Ctrl+A` 被当成别的命令/补全吃了 → 否掉。
2. **怀疑 keybinding 没启用**：翻 pi-tui 源码，发现它 `ctrl+u → deleteToLineStart` 等都有定义，`getKeybindings()` 也正确填充了 → 不是没启用。
3. **怀疑是库匹配 bug**：我用单测把 `matchesKey('\x15','ctrl+u')` 测出来是 `true`，证明匹配函数本身没问题 → 又否掉。
4. **怀疑终端转发**：问用户（广山哥）是不是 iTerm2 键位设置问题 → 用户说 Claude 里 Ctrl+U 明明正常，说明 iTerm2 本身没问题。
5. **试过自定义 keybinding、手动把按键喂给编辑器** → 全部无效。

到这里我已经把「代码」「库」「终端」三层都怀疑了个遍，唯独漏了**输入数据的真实样子**。

## 决定性一步：采样按键的原始字节

我在 TUI 的按键监听里加了一段**临时采样**，把收到的裸字节打出来。结果真相大白：

```
Ctrl+A  →  ESC[97;1:3u
Ctrl+U  →  ESC[117;1:3u
Ctrl+W  →  ESC[119;1:3u
Ctrl+E  →  ESC[101;1:3u
←       →  ESC[D
```

**关键**：iTerm2 对这个 TUI **启用了 kitty keyboard protocol** —— `Ctrl` 组合键不是发标准单字节控制符（`\x01`、`\x15`），而是发成 `CSI-u` 序列（`\x1b[<码>;...u`）！

而 pi-tui 的按键匹配：
- 对**方向键**的 kitty 序列（`\x1b[1;1:3D` → `left`）**能识别** → 所以左右箭头正常 ✅
- 对**`Ctrl+字母`** 的 kitty 序列（`\x1b[97;1:3u` → `ctrl+a`）**不识别**（`matchesKey` 实测返回 false）→ 所以 Ctrl+A/U/W/E 全部失效 ❌

**也就是说：不是代码错了，不是库错了，也不是终端配错了——是 iTerm 用了 kitty 协议发 Ctrl，而 pi-tui 恰好没实现对 'Ctrl+字母' 的 kitty 序列匹配。三层都是"各自作怪但单看又都对"，合起来就崩。**

## 修复：把 kitty 序列翻译回单字节控制符

既然 pi-tui 认单字节控制符、只是不认 kitty 的 `CSI-u` 形式，我一共建一条**翻译桥**：在按键监听里解析 `\x1b[<字母码>;...u`，把 `'a'→0x01`、`'u'→0x15`、`'w'→0x17`、`'e'→0x05`……映射回单字节控制符，再喂回编辑器用它内置的行编辑逻辑。

验证通过后，`Ctrl+A/E/U/K/W/B/F/D` 全部生效了。

## 复盘 / 教训

1. **这类纵深 bug，越早看"输入到底是什么"，越早定位**。我在"代码→库→终端"三层之间怀疑了很久，唯独没第一时间做字节级采样——这一步本应是第一步。
2. **终端协议差异是隐性手雷**。kitty keyboard protocol / CSI-u 这种"终端发送编码"层面的差异，代码里很难直接看出来，只有看原始字节最有效。
3. **Don't assume，inspect**：`matchesKey`/`keybinding` 各写各的，单测通过不代表真实按键数据能匹配——真实的数据形态（kitty 序列）跟单测里的假设（单字节）根本不是一回事。

## 顺带（这次的 REPL 增强清单最终都落地了）

- `--web-sessions`：让 TUI 和 web 共用同一份会话存储根
- `/tts` 系列加入命令补全提示
- 上下/下箭头翻历史
- `Ctrl+...` 行编辑（本次主角）
- 另外修了两次"会话日志 seq gap 损坏"（都是强杀进程导致的）

一句真话：**越难查的 bug，越要回到"输入端到底收到了什么"去看。** 🐳

---

## 追加：kitty 键盘协议带来的连锁 bug（事后归因）

今天后来把所有问题逐条对了一遍，**同一个 iTerm 的 kitty 键盘协议，单独就引爆了 4 个 bug**：

1. **`Ctrl+A/E/U/K/W` 行编辑完全不生效**：pi-tui 认不了 iTerm 发来的 kitty `Ctrl+字母` 序列（`ESC[97;9u`）。
2. **`Ctrl+C` 一按就全退**：iTerm 把一次 Ctrl+C 拆成 **Press（`ESC[99;9u`）和 Release（`ESC[99;9:3u`，`:3`=kitty Release 标志）** 两个事件。第一条清空了输入框、第二条因输入框已空被误判成"空输入 → 退出"。
3. **中文输入乱序/被换行**：早期按 modifier 不加区分，把 IME 伴随序列（modifier=`1`，如 `ESC[107;1:3u`）误当成 Ctrl 命令处理，打破了中文。
4. **"吞字/清空"**：早期 `Ctrl+C` 检测把某些真实 key（普通 `c` 等）误判，导致已输入的文本被清空。

**而其它 5 个 bug 和 kitty 无关**：`/tts` 补全漏加、会话日志 seq gap（强杀进程损坏）、上箭头历史没接 `addToHistory`、队列"只发一条"（其实是日志损坏掩盖）、TUI/web 各管各会话（架构）。

**关键教训**：一套"看着正常"的终端协议，能在多个毫不相干的表象下埋下连环雷。只有当破案需要去抓"按键真实字节（含 Press/Release 事件类型、modifier、IME 伴随）"时，才把这些全部串起来。**终端层协议 + 输入法 + TUI 解析，是一块最容易"看着都对、合起来崩"的深水区。**

另外确认：`ESC[97;9u` 的 `9` 是 modifier（Ctrl 相关），`:3` 才是 Release 标志——所以"普通 vs Ctrl"看 modifier，"按下 vs 释放"看 `:N` 事件标志。
