# dsh-repl TUI 开发记录 · 2026-08-15 总结

> 广山哥 · 一次性完成的 REPL 增强 + 一次深水区踩坑

## 代码量

本会话核心功能合并净改动（4 笔提交，9 个文件）：

- **新增 547 行，删除 28 行**
- 涉及源码：`apps/repl/src/tts.ts`（新增 TTS）、`apps/repl/src/tui-repl.ts`（TUI 主逻辑）、`apps/repl/src/bin.ts`（入口参数）
- 测试：`apps/repl/tests/tts.spec.ts`（新增）
- 配置：`vitest.config.ts`

## 功能清单（落地并提交推送）

| # | 功能 | 说明 |
|---|------|------|
| 1 | **TTS 朗读 `/tts`** | Edge TTS 合成 + 本地播放；`/tts on/off` 自动朗读、`/tts status`、`/tts <文>`、`/tts default` |
| 2 | **TTS 临时文件自动清理** | 播放后删 MP3，不再残留 |
| 3 | **`--web-sessions` 参数** | TUI 切到 dsh web 的共享会话根，与 web 读写同一份日志 |
| 4 | **`/tts` 命令补全** | 敲 `/t` 有提示（含元命令），提示改黄色更醒目 |
| 5 | **上/下箭头翻历史** | 记录每次输入，↑/↓ 回拉复用 |
| 6 | **忙时消息队列** | 回复中继续提交→排队显示次序，每轮结束自动连发 |
| 7 | **Ctrl+... 行编辑** | Ctrl+A/E/U/K/W/B/F/D（行首/行尾/删行/删词等）适配 iTerm kitty |
| 8 | **Ctrl+C 三级修复** | 忙时中断 / 空闲有字清空 / 空退出（修好 kitty 双事件误判） |

## 顺手解决的环境问题

- 多次 **会话日志 seq gap 损坏** → 全场重编号修复（含一条 5.3MB、31 万事件的大会话）
- **kitty 键盘协议**踩坑：普通 vs Ctrl 看 modifier（=9 是 Ctrl）、按下 vs 释放看 `:3`（Release）、IME 伴随噪声（modifier=1）区分

## 提交记录

```
f263e9ce2b  feat(repl): queue busy messages and robust kitty Ctrl handling
5fc1c741c9  feat(repl): add --web-sessions, TTS completion, history, kitty line-edit
c81364b06e  fix(repl): remove synthesized TTS temp file after playback
bca9dea825  feat(repl): add Edge TTS read-aloud (/tts) for the TUI
```

## 次要环境改动（不走仓库）

- `launch-tui.sh`（个人启动脚本，默认自动起 web 3080 + `--web-sessions`）
- `~/.zshrc` 加 `ygsw` 快捷（一键 TUI + web）
- 两篇笔记上传 Knowly

一句话：TUI 从"基本可用"升级到"能朗读、能排队、行编辑顺手、和 web 共享会话"，代码净增约 550 行。🐳
